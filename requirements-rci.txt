# GloPose requirements for RCI (foss-2025b toolchain)
#
# Prerequisites provided by HPC modules (DO NOT pip install these):
#   PyTorch 2.8.0, torchvision 0.23.0, torchaudio 2.8.0 (CUDA 12.9.1)
#   numpy>=2, scipy, scikit-image, pillow, networkx, imageio, tqdm, matplotlib
#   COLMAP 3.13 (CLI only — pycolmap must be pip-installed separately)
#
# HPC modules file: ~/scripts/modules_glotracker_foss2025b.txt
# Venv setup: python -m venv --system-site-packages ~/envs/glopose
#
# Install: pip install -r requirements-rci.txt

# --- Core dependencies ---
pycolmap==3.13.0
kornia
kornia_moons
gradio
wandb
huggingface_hub
rerun-sdk>=0.22
trimesh
ultralytics
opencv-python
einops
safetensors
imbalanced-learn

# --- Kaolin (build from source for torch 2.8.0) ---
# pip install "Cython>=3.1"
# git clone --recursive https://github.com/NVIDIAGameWorks/kaolin.git
# cd kaolin && python setup.py develop

# --- bop_toolkit (skip deps to avoid numpy<2 and pillow<=9.5 conflicts) ---
# pip install --no-deps --ignore-requires-python "bop_toolkit_lib @ git+https://github.com/thodan/bop_toolkit.git"
# Then install its runtime deps manually:
pypng
pytz
webdataset
PyOpenGL
vispy
pycocotools @ git+https://github.com/MartinSmeyer/cocoapi.git#subdirectory=PythonAPI
hand-tracking-toolkit @ git+https://github.com/facebookresearch/hand_tracking_toolkit.git

# --- Editable installs from repositories/ ---
# pip install -e repositories/sam2
# pip install -e repositories/vggt    (use --no-deps to skip numpy<2 pin)
# pip install -e repositories/mast3r
# pip install -e "git+https://github.com/Parskatt/RoMa.git#egg=romatch"
# pip install -e "git+https://github.com/UniFlowMatch/UFM.git#subdirectory=UniCeption&egg=uniception"
# pip install -e "git+https://github.com/UniFlowMatch/UFM.git#egg=uniflowmatch"

# --- Optional (for segment-anything v1, if needed) ---
# segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
